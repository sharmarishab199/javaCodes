import java.util.*;
public class _9collections{
    public static void main(String[] args) {
        //PriorityQueue<Integer> pq=new PriorityQueue<>();pq.add(67);pq.remove(100);pq.contains(5)
        //PriorityQueue<Integer> pq1=new PriorityQueue<>(Collections.reverseOrder());pq.poll();
        //PriorityQueue<Integer> copied=new PriorityQueue<>(pq);

        //TO ARRAY
        // PriorityQueue<Integer> pq=new PriorityQueue<>();
        // pq.add(67);pq.add(55);pq.add(23);pq.add(7);
        // Integer[] array = pq.toArray(new Integer[pq.size()]);
        // System.out.println(array[0]);

        // Integer to int
        // Integer integerArray[]={2,7,2,4,5};
        // int[] intArray = Arrays.stream(integerArray).mapToInt(Integer::intValue).toArray();
        // for(int i:intArray){
        //     System.out.print(i+" ");
        // }


        // ArrayList<Integer> list=new ArrayList<>();
        // list.add(96);list.add(21);list.add(0,42);
        // list.get(0);list.remove(0);list.set(0,45);
        // System.out.println(list);
        // // Add all elements from list2 to list1
        // list1.addAll(list2);

        // Create a copy of list1 (list2)
        //List<Integer> list2 = new ArrayList<>(list1);
        
        //list to set
        //Set<Integer> set = new HashSet<>(list);


        //HASHMAP
        //Map<Integer,Integer> map=new HashMap<>();
        // map.put(1,2);map.put(5, 1);map.put(3,2);map.put(99, 1);
        // for(int i:map.keySet()){
        //     System.out.println(i+"=>"+map.get(i));
        // }
        // System.out.println(map.containsKey(1));

        // map.put(2,map.getOrDefault(map, 0)+1);
        // System.out.println(map);
    }
}